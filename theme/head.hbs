{{!-- Glide theme custom head.
     Set a version label here to show the pill next to the book title in the
     header (the theme's glide.js reads it). Remove the meta to hide the pill. --}}
<meta name="glide-version" content="v2.4">
